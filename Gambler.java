
public class Gambler extends Hero {
	/**
	 * Calls super class (Hero) constructor.
	 * sets the hero's name, type, health, recovery rate
	 */
	public Gambler(String inputName) {
		
		super(inputName, "Gambler", 150, 20);
	}
}
