
public class Tank extends Hero{
	/**
	 * Calls super class (Hero) constructor.
	 * Sets the hero's name, type, health, and recovery rate.
	 */
	public Tank(String inputName) {
		super(inputName, "Tank", 400, 10);
	}
}
